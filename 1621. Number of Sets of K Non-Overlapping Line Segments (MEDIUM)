class Solution:
    def numberOfSets(self, n: int, k: int) -> int:
        return comb(n+k-1,2*k)%(10**9+7)
        
        
        
        
class Solution {
   /* vector<vector<long long>>dp;
    int topdown(int n,int k)
    {
        long long mod=1e9+7;
        if(k==0&&n>=0)
            return 1;
        if(k>n-1)
            return 0;
        if(dp[n][k]!=-1)
            return dp[n][k];
        dp[n][k]=0;
        for(int i=n-1;i>=1;i--)
        {
            dp[n][k]+=topdown(i,k-1)*(long long)(n-i);
            dp[n][k]%=mod;
        }
        return dp[n][k];
    }*/
public:
    int numberOfSets(int n, int k) {
       /* dp.resize(n+1,vector<long long>(k+1,-1));
        return topdown(n,k);*/
       
    }
};

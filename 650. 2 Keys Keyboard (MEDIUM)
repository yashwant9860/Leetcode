class Solution {
public:
    int minSteps(int n) {
        int dp[n+1];
        fill_n(dp,n+1,INT_MAX);
        dp[1]=0;
        for(int i=2;i<n+1;i++)
            for(int j=1;j<i;j++)
                if((i)%j==0)
                    dp[i]=min(dp[i],dp[j]+1+(i-j)/j);
        return dp[n];
    }
};
